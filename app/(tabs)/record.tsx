import { View } from 'react-native';

/**
 * Dummy tab screen required by expo-router.
 * The CenterTabButton intercepts press and navigates to /record instead.
 */
export default function RecordTab() {
  return <View />;
}
